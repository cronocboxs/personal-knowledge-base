# AGENTS.md - Root Guidelines

当リポジトリ（`personal-knowledge-base`）は、AIエージェントおよび自動化スクリプトとの協調運用を前提とした構造化ナレッジベースです[cite: 3]。
すべてのAIエージェント（Claude, Cursor, Copilot, 独自スクリプト等）は、本ファイルの絶対ルールおよび `00-rules/` 内の各規約の遵守を最優先とします[cite: 3]。

---

## 1. 絶対遵守ルール（Core Mandatory Rules）

1. **最高機密の保護**: `01-private/` 内の情報（個人情報・認証情報等）を外部へ出力・送信・要約露出し落としてはならない (`.gitignore` 対象)[cite: 3]。
2. **原本の非破壊**: `04-resources/` 内の一次情報原本は絶対に書き換え・修正・直接削除しないこと[cite: 3]。
3. **ファイル移動の徹底**: ファイルの直接物理削除（`rm`）は禁止。不要ファイルは必ず `99-trash/` へ移動すること[cite: 3]。
4. **事前承認の義務**: ディレクトリ構造の変更、大量ファイルの移動・一括書き換え時は、必ず事前にユーザーの承認を得ること[cite: 3]。

---

## 2. ディレクトリ構造と編集ポリシー（Directory Index）

| ディレクトリ | 役割 | エージェントの編集権限・ルール |
| :--- | :--- | :--- |
| `00-rules/` | システム規約・フォーマット | **参照専用**（ユーザー指定時のみ更新）[cite: 3] |
| `01-private/` | 個人情報・秘密情報 | **最高機密**: `.gitignore` 対象。外部送信・要約出力厳禁[cite: 3] |
| `02-knowledge/` | 自分に関する事実、体系化された知見・概念メモ | 新規作成・追記（構造化されたMarkdown）。`04-resources/`から書き起こす[cite: 3] |
| `03-output/` | 作った成果物・記事・コード | 生成物・ドラフトの配置。`02-knowledge/`の内容から作成する[cite: 3] |
| `04-resources/` | 参照資料・Web素材・一次情報の原本 | 入力データの保管・参照・要約元の配置。書き換えは行わない[cite: 3] |
| `05-todo/` | タスク・プロジェクト | ステータス更新・タスク追加・ロードマップ[cite: 3] |
| `06-storage/` | 動画・PDF等の大容量ファイル・バイナリ | `.gitignore` 対象。Gitで管理不能な大容量データ置き場（外部同期前提）[cite: 3] |
| `99-trash/` | 一時廃棄 | 不要ファイルの退避場所（直接削除禁止）[cite: 3] |
| `scripts/` | 自動化・メンテナンス | 整理・更新・検証スクリプトの配置・実行[cite: 3] |
| `.agents/skills/` | エージェント手順書・スキル定義 | エージェント用タスク定義ファイルの参照・機能追加・メンテナンス[cite: 3] |
| `.agents/prompts/` | システムプロンプト・ペルソナ定義 | エージェントの役割・振る舞い・思考スタンスの定義参照 |
| `.agents/templates/` | 出力フォーマット雛形 | 記事やナレッジ作成時に利用する標準レイアウトの参照 |
| `.agents/memories/` | エージェント長期記憶・ログ | 会話文脈、ユーザーの好み、試行錯誤のフィードバック保持 |

---

## 3. 詳細規約インデックス（Detailed Rules）

具体的な運用手順やフォーマット規定については、作業内容に応じて以下の規約を参照してください[cite: 3]。

* **情報ライフサイクル・フロー規約**: [`00-rules/workflow.md`](./00-rules/workflow.md)[cite: 3]
* **ファイル分類・配置判定規約**: [`00-rules/classification.md`](./00-rules/classification.md)[cite: 3]
* **ファイル命名・記述・メタデータ規約**: [`00-rules/formatting.md`](./00-rules/formatting.md)[cite: 3]
* **エージェント行動規範・連携規約**: [`00-rules/agent-behavior.md`](./00-rules/agent-behavior.md)[cite: 3]
* **プライバシー・セキュリティ規約**: [`00-rules/privacy-security.md`](./00-rules/privacy-security.md)[cite: 3]