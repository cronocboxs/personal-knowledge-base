# スクリプト仕様

```plaintext
./scripts/fetch-git-log.sh <GitHub_URL> <ブランチ名> を実行して 04-resources/git-repository/<Repository>/<branch> に clone / fetch し、04-resources/git-repository/<Repository>/history/ にログを格納させる。
```
